# Nome da aplicacao Spring Boot
spring.application.name=subip-mg-backend

# Configuracao do banco MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/subip_mg
spring.datasource.username=root
spring.datasource.password=11373137
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracao do JPA
spring.jpa.hibernate.ddl-auto=create

# Configuracoes de exibicao de SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
